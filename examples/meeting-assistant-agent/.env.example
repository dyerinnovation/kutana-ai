# Anthropic API key (required)
ANTHROPIC_API_KEY=sk-ant-...

# Convene API key (required)
# Get from: Settings → API Keys → Generate Key (scope: Agent)
CONVENE_API_KEY=cvn_...

# Convene MCP server URL (optional — defaults to http://convene.spark-b0f2.local/mcp)
# CONVENE_MCP_URL=https://your-convene-host.com/mcp
