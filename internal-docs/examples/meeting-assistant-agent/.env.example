# Anthropic API key (required)
ANTHROPIC_API_KEY=sk-ant-...

# Kutana API key (required)
# Get from: Settings → API Keys → Generate Key (scope: Agent)
KUTANA_API_KEY=cvn_...

# Kutana MCP server URL (optional — defaults to http://kutana.spark-b0f2.local/mcp)
# KUTANA_MCP_URL=https://your-kutana-host.com/mcp
