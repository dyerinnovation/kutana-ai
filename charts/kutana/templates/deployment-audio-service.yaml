{{- if .Values.audioService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audio-service
  labels:
    {{- include "kutana.labels" . | nindent 4 }}
    app.kubernetes.io/name: audio-service
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: {{ .Values.audioService.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: audio-service
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: audio-service
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: audio-service
          image: {{ include "kutana.image" (dict "root" . "svc" "audio-service" "tag" .Values.audioService.image.tag) }}
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8001
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "kutana.fullname" . }}-config
            - secretRef:
                name: {{ include "kutana.fullname" . }}-secret
          {{- range $key, $value := .Values.audioService.env }}
          env:
            - name: {{ $key }}
              value: {{ $value | quote }}
          {{- end }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 30
            timeoutSeconds: 5
          resources:
            {{- toYaml .Values.audioService.resources | nindent 12 }}
{{- end }}
